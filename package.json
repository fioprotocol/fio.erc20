{
  "name": "fio.erc20",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npx truffle compile",
    "migrate-dev": "npx truffle migrate --network development",
    "migrate-rinkeby": "npx truffle migrate --network rinkeby",
    "migrate-mainnet": "npx truffle migrate --network mainnet",
    "test": "npx truffle test",
    "dev": "npx truffle console --network development",
    "dev-rinkeby": "npx truffle console --network rinkeby",
    "start-network": "npx ganache-cli"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.3.0",
    "@openzeppelin/contracts-upgradeable": "^4.4.1",
    "@openzeppelin/truffle-upgrades": "^1.12.0",
    "@truffle/hdwallet-provider": "^1.7.0",
    "dotenv": "^10.0.0",
    "solc": "^0.8.4",
    "truffle": "^5.1.65",
    "truffle-assertions": "^0.9.2"
  }
}
